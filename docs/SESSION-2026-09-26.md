# 세션 기록 2026-09-26

이어서 할 사람이 바로 붙을 수 있게 적는다. 마감 **2026-09-28 23:59**, 목표 제출 18:00.
이 문서를 쓰는 시점에 2일 8시간 남았다. 오늘 커밋 18건.

## 지금 막힌 것 하나

**팀명이 정해지지 않았다.** 구글 폼 제출이 9-27 까지이고 다섯 명이 각자 낸다. 팀명을 한 글자라도
다르게 적으면 한 팀으로 묶이지 않는다.

| 사람 | 의견 |
|---|---|
| 팀원 D | FlyGate |
| 팀원 A | FlyVigilance, FlyVigilante |
| 나머지 둘 | 없음 |

**권고는 FlyVigilance 다.** 프로젝트명 FlyGate 는 README 와 영상 나레이션과 그림 열다섯 군데에
들어 있어 그대로 두고, 팀명에 약물감시의 도메인 단어인 vigilance 를 쓴다. 두 사람 의견이 모두
살고 재렌더도 없다. 비질란테는 법 밖에서 사적으로 제재하는 사람이라는 뜻이라 규제 도메인에서
걸릴 수 있으므로 뺀다.

정해지면 세 곳을 맞춘다. `docs/submission-flygate.md` 의 파일명 규칙,
`scripts/make_submission_pdf.py` 의 `TEAM_NAME`, 제출 파일명이다.
공지 초안은 `_local/team-messages/` 의 역할 조정 문서에 이름 자리만 비워 두었다.

## 오늘 확정된 것: 푸는 범위

팀 논의로 정리됐다. 발표와 제출물에 이 범위로 쓴다.

**풀지 않는다.** 케이스 처리, 15일 보고 기한 대응, 노이즈 제거, 인력 부족. 이미 하는 곳이 많고
우리가 재지도 않았다. 푼다고 적으면 우리 크리틱이 반려할 주장이 된다.

**푼다.** 근거를 인용한 안전성 주장이 그 근거를 넘어섰는지 가려낸다.

**AI 로 한정하지 않는다.** 모아 둔 현장 사례 셋 중 AI 가 만든 문제가 하나도 없다. 근거와 사례는
`docs/notes/real-world-cases.md` 에 있다.

## 오늘 한 일

### 버그 셋을 고쳤다

**가드레일 토픽 게이트가 옛 후보만 허용하고 있었다.** 도킹 질의가 허용 목록에 없어 시연에서
막힐 수 있었다. FlyGate 범위로 다시 쓰고 `nemoguardrails` 로 로드해 확인했다.

**맥의 `core.ignorecase` 때문에 `.gitignore` 의 대문자 패턴이 소문자 파일까지 잡았다.**
`docs/` 아래 새 문서가 조용히 빠지고 있었다. 루트 앵커를 붙여 고쳤다.

**문서 수치가 출처와 어긋났다.** Night Shift 제거로 테스트가 293건인데 네 곳이 330 으로
남아 있었다. 근거를 넘어선 주장을 걸러낸다면서 우리 수치가 출처와 다르면 앞뒤가 맞지 않는다.

### 다시 어긋나지 않게 대조 스크립트를 만들었다

`scripts/verify_numbers.py` 가 항목 아홉 개를 출처에서 직접 재고 문서에 실려 있는지 본다.
종료 코드가 어긋난 항목 수라 제출 전 점검에 건다. 지금 아홉 건 모두 일치한다.

오늘 하루에 이 숫자가 313, 330, 358, 293 으로 네 번 바뀌었고 한 번은 문서 한 곳을 빠뜨렸다.

### 측정 둘

**크리틱 3단 A/B 기준선.** `scripts/bench_critic_judge.py` 로 33건을 두 번 쟀다.
확정본은 `eval/results/bench_nemotron-super-run2.json` 이다. 적발 16/16, 거짓 양성 0/17,
지연 중앙값 2,243ms, 건당 출력 토큰 270개다. 두 실행 사이 지연 중앙값이 1.26배 벌어졌으므로
다른 판정기와 견줄 때 한 번 재고 끝내면 안 된다.

**건수 많은 경로.** `scripts/bench_triage_scale.py` 로 니라파립 이상사례 10건을 돌렸다.
건당 출력 토큰 46.6개, 지연 중앙값 1,672ms 다. **이 표본에서는 모델이 보탠 것이 없다.**
10건 모두 모델과 고정 규칙이 같은 답을 냈다. 보고 건수 상위권만 뽑아 판정이 갈릴 수 없는
집합이었다. 집계를 다시 하니 11건에서 99건 사이에 394종이 있고 거기는 아직 안 돌렸다.

### 정리와 산출물

- Night Shift 자산 11개를 main 에서 빼고 `archive/nightshift` 태그에 보관했다
- 3패널 데모 화면 `assets/web/index.html`. https://claude.ai/artifact/4LmfRcMMJyqBEBCxFNaWXX
- 한 장 요약 그림 `docs/figures/architecture_overview.png` 를 만들어 README 머리에 두었다
- 영상 2판. 나레이션의 DiffDock 소요를 4.0초에서 실측 4.1초로 맞췄다. 176.1초
- 신청서 문안을 확정 범위로 다시 썼다. 문제 323자, 솔루션 480자로 둘 다 기준 안
- 새 문서 넷: `real-world-cases.md`, `skill-packaging.md`, `pv-datasets.md`, `triage-scale.md`,
  `jev-primer.md` 와 블로그 `jev-system-one-ko.md`

## 팀원에게 넘긴 것

| 사람 | 무엇 | 언제 |
|---|---|---|
| 팀원 A | 가드레일 토픽 게이트에 판정 전용 모델이 되는지 확인 | 9-27 |
| 팀원 A | 영상. 우리 2판은 대비책으로 둔다 | 9-28 오전 |
| 팀원 B | Agent Skill 패키징. 안내는 `docs/notes/skill-packaging.md` | 9-27 |
| 팀원 B | FAERS 11건에서 99건 구간 394종 | 9-27 |
| 팀원 C | 라벨 검토 | 9-27 낮 |
| 팀원 D | README 검토 | 9-28 오전 |

팀원 B 와 C 의 본선 참석 여부는 아직 모른다.

## 남은 일

| 무엇 | 누가 | 비고 |
|---|---|---|
| 팀명 확정과 공지 | 팀장 | **오늘 밤까지.** 폼이 내일 마감 |
| DLI 강좌 수료 | 전원 | 교육 미션. 4시간. 모듈 3, 4 는 Brev 인스턴스 필요 |
| 팀명 확정 후 PDF 와 파일명 | 나 | 세 곳을 한 번에 |
| 영상 3판 | 나 | 팀원 A 클립이 오면 26초에서 47초 구간 교체 |
| DLI 수료증 PDF 삽입 | 나 | `CERT_IMAGE` 가 비어 있다 |
| 제출 전 전체 검증 | 나 | `verify_numbers.py` 와 `pytest` |

## 판단이 필요한 것 셋

파일 분류에서 남은 항목이다. 제출 뒤로 미뤄도 된다.

| 파일 | 왜 애매한가 |
|---|---|
| `docs/submission-draft.md` | 구버전 신청서인데 현행 문안이 이 파일의 체크리스트를 참조한다 |
| `src/harness/tools/pharmasignal_cases.py` | 스크립트는 죽었는데 출력 JSON 을 살아 있는 그림 생성기가 읽는다 |
| `assets/scripts/record_demo.py` | 옛 CLI 를 불러 실행이 실패한다. 영상 3판을 만들면 고치고 아니면 버린다 |

## 조심할 것

- `assets/video/slides.py` 는 현행 생성기와 출력 폴더가 같다. 실행하면 지금 슬라이드 13장을
  덮어쓴다
- `git clean -fdx` 를 쓰면 `_local/` 이 통째로 날아간다. 팀 메시지는 복구할 수 없다
- 과잉해석 규칙을 16종으로 늘리는 것은 제출 뒤에 한다. 15종이 README, 신청서, PDF, 나레이션에
  박혀 있다
- 케이스 브리프를 `--offline` 으로 재생성하면 온라인 LLM 판정과 반려 사유 원문이 사라진다.
  오늘 한 번 겪었고 `git checkout` 으로 되돌렸다

## 지금 상태

| 항목 | 값 |
|---|---|
| 오프라인 테스트 | 293건 통과 |
| 설정 검증 | author, critic, eval, guardrails 네 개 통과 |
| 문서 수치 대조 | 9건 일치 |
| 작업 트리 | 깨끗. 원격과 동기 |
