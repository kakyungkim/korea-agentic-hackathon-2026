# 신청서 문안: FlyGate

작성 2026-09-25. 글자 수는 파이썬 `len()` 으로 쟀고 공백 포함과 미포함을 함께 적는다.
기준은 문제 300자, 솔루션 500자이며 ±10퍼센트 안에 맞췄다.
수치는 `docs/HANDOFF.md` 의 검증된 수치 표에서만 가져왔다.

| 문항 | 공백 포함 | 공백 미포함 | 기준 | 판정 |
|---|---|---|---|---|
| 문제 | 295 | 218 | 270~330 | 충족 |
| 솔루션 | 488 | 376 | 450~550 | 충족 |
| 기술 스택 | 아래 참조 | | 상한 미고지 | 해당 없음 |

문체는 합니다체다. 분량을 더 줄여야 하면 종결만 "~한다" 로 바꾸면 문항당 20자 안팎이 줄어든다.

## 서비스 명

```
FlyGate
```

이름은 2026-09-25 에 팀장이 정했고 팀원 의견을 받아 바꿀 수 있다. 초파리라는 훅과 검증 관문을
한 단어에 담았다. 선행 프로젝트 PharmaSignal 과 같은 조어 규칙이다.

## 해결하고자 했던 문제 (Problem Definition)

```
신약 후보를 계산으로 거를 때 도킹 점수를 씁니다. 그런데 이 점수에는 도구 문서가 직접 밝힌 해석 한계가 있습니다. 서로 다른 단백질에서 나온 점수는 견줄 수 없고, 딥러닝 도킹이 내는 신뢰도 값은 결합 세기가 아니며, 교차 도킹 결과는 결합의 증거가 아닙니다. LLM 에이전트를 이런 도구에 붙이면 그 선을 자주 넘습니다. 근거도 제대로 붙고 숫자도 로그와 맞는데 결론만 틀린 요약이 나옵니다. 형식을 보는 검사로는 걸러 낼 수 없어 사람이 매번 한계를 기억해야 하고, 놓치면 없는 선택성이나 없는 결합이 보고서에 남습니다.
```

(현재 295자, 공백 미포함 218자)

## 서비스 소개 및 주요 기능 (Solution)

```
FlyGate는 후보물질 하나를 구조에서 사람까지 이어 검증하는 에이전트입니다. 타깃 단백질과 후보를 받아 NVIDIA DiffDock과 AutoDock Vina 실측과 초파리 커넥톰 세 경로로 결합을 보고, 실험 친화도를 참조한 뒤, 그 후보가 사람에게서 어땠는지를 허가 라벨과 이상사례 보고와 문헌으로 확인합니다. 모든 주장에 근거 ID가 붙습니다. 크리틱이 세 단계로 검증합니다. 1단은 근거 ID 유무를 보고, 2단은 숫자를 원본 로그와 대조하며, 3단은 도구 문서가 금지한 추론을 했는지 판정합니다. 규칙 열다섯 가지는 지어낸 것이 아니라 도구 제공자와 데이터 제공자가 문서에 적은 경고에서 옮겼습니다. 평가 서른세 건에서 과잉해석 열여섯 건을 3단이 모두 반려했고, 거짓 양성은 없었습니다. 같은 케이스를 고정 규칙만으로 돌리면 한 건만 걸립니다. 그 차이가 의미 판단의 몫입니다. 모든 실행은 OpenShell 정책 안에서 허용 호스트 일곱 곳만 열고 돕니다.
```

(현재 488자, 공백 미포함 376자)

**초파리 경로 주의.** 위 문안에 "초파리 커넥톰 세 경로" 가 들어 있다. `flybrain_pose` 도구는
제출 시점에 구현 여부가 정해지지 않았다. **구현되지 않으면 이 구절을 빼고 "두 경로" 로 고친다.**
그 경우 공백 포함 기준으로 약 12자가 줄어 476자가 되고 기준 안에 그대로 있다.

## 활용한 핵심 기술 및 AI 모델 (Tech Stack)

```
NVIDIA Nemotron 3 Super 120B(nvidia/nemotron-3-super-120b-a12b)를 계획과 크리틱 판정에, Nemotron 3.5 Lightning 30B(nvidia/nemotron-3.5-lightning-30b-a3b)를 반복 작업에 씁니다. 두 모델 모두 build.nvidia.com NIM API(OpenAI 호환)로 호출합니다.

NVIDIA DiffDock NIM(health.api.nvidia.com/v1/biology/mit/diffdock)으로 단백질과 리간드의 결합 포즈를 예측합니다. 응답의 신뢰도 값은 포즈 타당성 지표이므로 결합 친화도로 환산하지 않습니다. NVIDIA 문서가 밝힌 한계를 크리틱 규칙에 그대로 넣었습니다.

NVIDIA NeMo Agent Toolkit 1.9.0으로 작성자 워크플로와 크리틱 워크플로를 분리했습니다. 도메인 도구 여덟 종을 NAT 함수로 등록해 에이전트가 직접 호출하고, nat eval로 크리틱 적발률을 측정합니다. 크리틱 워크플로에는 쓰기 도구가 없습니다. NeMo Guardrails 미들웨어로 가드레일 계층을 배선했습니다.

NVIDIA OpenShell 0.0.116으로 실행을 가둡니다. deny-by-default 정책으로 허용 호스트 일곱 곳만 열고, 쓰기는 지정한 출력 폴더만 허용하며, 그 밖의 접근은 차단 로그로 남습니다. 스모크 검증 열여덟 건이 모두 통과했습니다.

build.nvidia.com 스킬 카탈로그의 NVIDIA BioNeMo 에이전트 스킬 문서를 NIM 호출 규격의 참조로 썼습니다.

그 밖에 Python 3.12, 공개 데이터 소스로 openFDA FAERS, DailyMed SPL, PubMed E-utilities, RCSB PDB를 씁니다. 불균형 지표(PRR, ROR, 신뢰구간, 카이제곱)는 모델이 아니라 파이썬이 계산합니다.
```

## 추가 URL (선택)

- 데모 영상 링크. 제출 직전 채운다
- GitHub 저장소. 제출 직전 공개로 돌린다

## 적지 않는 것 (정직성 규율)

이 저장소의 결정 사항이다. 되지 않는 것을 되는 것처럼 쓰지 않는다.

| 대상 | 왜 빼는가 |
|---|---|
| NemoGuard 판정 모델 | 호스팅 쪽 오류로 시연하지 못했다. 배선까지 했다는 사실만 적는다 |
| NeMo Retriever 리랭커 | 이 계정 모델 목록 여든두 개에 하나도 없다 |
| NemoClaw | 쓰지 않았다. 참조 스택으로 검토했다고만 적는다 |
| 샌드박스 안 DiffDock 호출 | TLS 가 통하는 것까지 확인했고 POST 는 부르지 않았다 |
| `jev_triage` | 구현 여부 미정이다. 확정되면 기술 스택에 한 줄 더한다 |
| 소요 시간 절감 | 출처가 없다 |

## 계보와 기여

README 와 발표에 함께 밝힌다. 자세한 것은 `docs/notes/credits.md`.

> 주제와 초파리 도킹 경로는 팀원 제안이고, 에이전트 하네스와 검증 체계는 선행 프로젝트
> PharmaSignal v0에서 이어 온 자산입니다. 두 층을 하나의 파이프라인으로 이었습니다.

초파리 도킹 자산은 팀원의 별도 저작물이다. 복제하지 않고 데이터 출처와 SHA256 을 밝혀 인용한다.
커넥톰 원본은 CC BY 4.0 이라 출처 표기가 필요하다.

## 파일명 규칙

형식은 `[NVIDIA 해커톤_팀명_프로젝트명]` 이다. 대괄호를 포함해 그대로 쓰고
팀명은 폼에 적은 철자와 띄어쓰기를 글자 하나까지 맞춘다.

```
[NVIDIA 해커톤_<팀명>_FlyGate].pdf
[NVIDIA 해커톤_<팀명>_FlyGate].mp4
```

**팀명이 정해지기 전까지는 아래 이름을 쓴다.** 해커톤과 프로젝트가 드러나고 판본과 날짜가
남는다. `final.mp4` 는 빌드 기본 출력명이라 그대로 내보내지 않는다. 1차본을 최종본처럼
부르게 된다.

```
NVIDIA해커톤_FlyGate_데모영상_v1_20260925.mp4
NVIDIA해커톤_FlyGate_데모영상_v1_20260925.srt
```

## 제출 전 점검

`docs/submission-draft.md:216-234` 의 체크리스트를 그대로 쓴다. 이 문안에만 해당하는 것을 더한다.

- 초파리 경로 구현 여부를 확인하고 솔루션 문안의 "세 경로" 를 맞춘다
- `jev_triage` 구현 여부를 확인하고 기술 스택을 맞춘다
- 적발률 수치를 판정 실패 0건인 실행으로 한 번 더 확인한다
- 모든 수치를 `docs/HANDOFF.md` 표와 대조한다
- kr-style-polish 로 윤문한다
