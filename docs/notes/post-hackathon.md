# 제출 후 계획: 세 갈래와 순서

2026-09-25 작성. 마감 9/28 18:00 제출을 마친 뒤에 할 일을 정리한다.
논문 상세는 `docs/notes/paper-plan.md`, 주제 결정 근거는 `docs/notes/topic-decision.md`에 있다.

## 먼저 인정할 것: 세 갈래는 서로 다른 것을 보상한다

같은 작업물이라도 노리는 곳에 따라 포장이 달라진다. 하나를 최적화하면 나머지가 손해를 본다.
그래서 **동시에 하지 않고 순서를 둔다.**

| 갈래 | 보상하는 것 | 우리 현재 상태 |
|---|---|---|
| 해커톤 심사 | NVIDIA 기술 심도, 완성도, 독창성, 재현 가능한 증거 | 여기에 맞춰 만들고 있다 |
| GitHub 스타 | 5초 안에 보이는 훅, 마찰 없는 실행, 배포 시점 | 맞지 않는다. 키와 VM 과 NAT 설치가 필요하다 |
| 논문 | 규모, 베이스라인, 일치도, 정직한 한계 | 케이스가 열 쌍 남짓이라 아직 부족하다 |

**README 하나로 셋을 다 받을 수 없다.** 스타용은 움직이는 그림 먼저에 설명 최소이고,
심사용은 증거와 수치와 재현 절차이고, 논문용은 한계와 베이스라인이다.
제출용 저장소는 심사용으로 두고 나머지는 **별도 저장소로 뗀다.**

## 시간 창

| 시점 | 무엇 | 이 기간에 할 일 |
|---|---|---|
| 9/28 18:00 | 온라인 사전 챌린지 제출 | 제출에만 집중. 다른 갈래를 건드리지 않는다 |
| 9/29 ~ 10/1 | 대기 | **여기가 스타 갈래의 창이다.** 사흘 |
| 10/2 | 본선 10팀 발표 | |
| 10/3 ~ 10/6 | 본선 준비 | 본선 진출 시 여기에 집중 |
| 10/7 | 오프라인 One-day 해커톤 | 팀원 한 명이 참석 불가. 역할 배분 필요 |
| 10/8 ~ 11/9 | 대기 | **여기가 논문 갈래의 창이다.** 한 달 |
| 11/10 | 상위 5팀 AI Day Seoul 무대 | 논문 초고가 있으면 발표에 무게가 실린다 |

본선에 못 가면 10/3부터 바로 스타와 논문으로 넘어간다. 그 경우 시간이 더 넉넉하다.

## 갈래 1: GitHub 스타

### 솔직한 전망

스타는 품질의 함수가 아니다. 훅과 마찰과 배포 시점의 함수다.
우리 하네스는 NVIDIA API 키와 리눅스 VM 과 NAT 설치를 요구한다. 마찰이 크면 아무도 누르지 않는다.

그래서 **떼어 낼 것을 정해서 뗀다.** 기대치는 현실적으로 잡는다.

### 후보 셋, 값어치 순

**1. NVIDIA 생물학 NIM 파이썬 클라이언트 (권고)**

쓸 만한 클라이언트가 지금 없다. 우리가 정리한 함정이 정확히 사람들이 걸리는 자리다.
DiffDock 의 `steps` 와 `num_steps`, Boltz-2 MSA 의 `alignment` 와 `data`, MolMIM 의 `smi`,
GenMol 의 문자열 `temperature`, 항상 null 인 `pae`, 429 백오프, 202 방어 분기다.
근거는 `docs/notes/bionemo-nim.md` 와 `eval/results/diffdock_*.txt` 에 있다.

- 어차피 `src/harness/tools/bionemo_client.py` 로 만들고 있어 떼어 내는 비용이 작다
- 마찰이 낮다. `pip install` 과 API 키 하나다
- 독자가 분명하다. 계산화학과 AI 신약개발 쪽
- 기대치는 수십에서 수백 개. 폭발은 기대하지 않는다
- 라이선스 주의. NVIDIA 스킬 문서는 CC-BY-4.0 이라 **출처 표기가 필요하다**

**2. 초파리 도킹 시각화 (팀원 A 의 결정 사항)**

가장 큰 훅이다. 이미 Doom 과 마인크래프트와 코인봇으로 크게 퍼진 계보 안에 있다
(`docs/notes/fddd-and-jev.md` 참고). README 맨 위에 움직이는 그림을 걸고 한 줄로 돌아가게
만들면 터질 자리다.

**다만 FDDD 는 팀원 A 의 저작물이므로 그분이 정한다.** 우리가 대신 공개하지 않는다.
제안만 전하고, 그분이 하기로 하면 우리 쪽에서 붙일 수 있는 것을 돕는다.

**3. 과잉해석 규칙과 케이스 데이터셋**

스타는 적지만 논문 부속물이 되어 값이 두 번 난다. 논문 공개 시점에 맞춰 낸다.

### 떼어 낼 때 지킬 것

- **제출용 저장소를 건드리지 않는다.** 별도 저장소로 복사하고 계보를 밝힌다
- 키와 팀원 실명이 이력에 남지 않는지 확인한다
- 저작자는 `kakyungkim <kakyung.kim@gmail.com>` 하나다
- 외부 자산 라이선스를 표기한다. 목록은 `docs/notes/credits.md`

### 하지 말 것

**지금 스타를 노려 제출용 README 를 바꾸지 않는다.** 심사에서 완성도를 잃는다.
그림 먼저에 설명 최소인 README 는 심사위원이 찾는 증거를 숨긴다.

## 갈래 2: 논문

상세는 `docs/notes/paper-plan.md`. 요지만 적는다.

**논문이 되는 것은 초파리가 아니라 과잉해석 크리틱이다.** 초파리 도킹은 지금 형태로는
표준 재도킹 벤치마크에서 Vina 나 DiffDock 과 겨룬 수치가 없고, 원 논평자도 한계를 못 박았다.
게다가 팀원 A 의 저작물이다.

기여는 하나로 선다. 기존 환각 평가는 근거의 사실성을 재는데, 우리는 **근거가 전부 맞을 때의
추론 타당성**을 잰다. 계산화학에서는 해석 한계가 도구 문서에 적혀 있어 정답 라벨이 주관적이지 않다.

## 순서 권고

1. **9/28 제출까지 다른 갈래를 건드리지 않는다.** 마감이 하드하다
2. 제출 직후 사흘(9/29~10/1)에 **NIM 클라이언트를 별도 저장소로 뗀다.** 하루면 된다
3. 본선 여부가 정해지면 본선에 집중하거나 논문으로 넘어간다
4. 10/8 이후 한 달에 **논문 확장.** 케이스 수백 쌍, 모델 여럿, 전문가 일치도, 베이스라인
5. arXiv 선공개 후 워크숍 투고. 자리 후보는 `docs/notes/paper-plan.md`

## 남은 공백

- [Gap:Procedural] FDDD 공개 여부는 팀원 A 의 결정이다. 제안만 전한다
- [Gap:Consideration] 본선에 가면 스타 갈래가 10/8 이후로 밀린다. 그래도 순서를 바꾸지 않는다
- [Gap:Assumption] 스타 기대치 수십에서 수백은 근거 없는 추정이다. 비슷한 규모의 도구
  저장소를 몇 개 대조해 보면 감이 잡힌다
